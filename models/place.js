class Place {
  constructor(id, title, imageUri, address, lat, lon) {
    this.id = id;
    this.title = title;
    this.imageUri = imageUri;
    this.address = address;
    this.lat = lat;
    this.lon = lon;
  }
}

export default Place;
